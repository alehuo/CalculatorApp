import {
  StyleSheet
} from 'react-native'

export default StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    backgroundColor: '#2196F3'
  },
  value: {
    textAlign: 'center',
    color: '#FFFFFF',
    fontSize: 30
  }
})
